# Environment Variables for LLM Integration
# NOTE: Gemini API key is configured via the LLM Config panel in the UI
# No environment variables are needed for the current setup

# OpenAI Configuration (not currently used)
# OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Configuration (not currently used)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Local LLM Configuration (not currently used)
# LOCAL_LLM_URL=http://localhost:11434/api/generate
# LOCAL_LLM_MODEL=llama2